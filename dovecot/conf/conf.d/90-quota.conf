# Quota — Dovecot 2.4 named quota block syntax

mail_plugins {
  quota = yes
}

protocol imap {
  mail_plugins {
    imap_quota = yes
  }
}

quota user {
  storage_size = 0
}

quota_exceeded_message = Mailbox is full — over storage quota


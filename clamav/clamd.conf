# ClamAV daemon configuration

# Server socket (required)
LocalSocket /run/clamav/clamd.ctl

# Scan size limits — prevent memory exhaustion on large attachments
MaxFileSize 25M
MaxScanSize 100M
MaxRecursion 10
MaxFiles 10000

# Stream buffer size for milter/socket scanning
StreamMaxLength 25M
